{
  "name": "github-claw-dev-line-bot-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.js --outfile ./dist/index.mjs --target browser --format esm",
    "dev": "node ./scripts/run-wrangler-with-worker-name.mjs dev",
    "deploy": "bun run build && node ./scripts/run-wrangler-with-worker-name.mjs deploy",
    "set": "node ./scripts/run-wrangler-with-worker-name.mjs secret bulk .dev.vars",
    "test": "node --test"
  },
  "dependencies": {
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "node-addon-api": "^8.6.0",
    "node-gyp": "^12.2.0",
    "wrangler": "^4.75.0"
  }
}
